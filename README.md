# Application Design Patterns(java examples)
🛠Creational 🏗Structural 🧠Behavioral patterns examples and description on JAVA.

## 🛠 Порождающие (Creational)

Отвечают за удобное и безопасное создание объектов, скрывая логику инстанцирования.

- **Одиночка (Singleton):** Гарантирует, что у класса есть только один экземпляр, и предоставляет к нему глобальную точку доступа.
- **Фабричный метод (Factory Method):** Определяет интерфейс для создания объекта, но оставляет подклассам решение о том, какой класс инстанцировать.
- **Абстрактная фабрика (Abstract Factory):** Позволяет создавать семейства связанных объектов без привязки к их конкретным классам.
- **Строитель (Builder):** Позволяет создавать сложные объекты пошагово.
- **Прототип (Prototype):** Позволяет копировать объекты, не вдаваясь в подробности их реализации.

---

## 🏗 Структурные (Structural)

Отвечают за построение удобных иерархий классов и связей между объектами.

- **Адаптер (Adapter):** Позволяет объектам с несовместимыми интерфейсами работать вместе.
- **Мост (Bridge):** Разделяет абстракцию и реализацию, чтобы они могли изменяться независимо.
- **Компоновщик (Composite):** Позволяет сгруппировать объекты в древовидную структуру и работать с ней так же, как с единичным объектом.
- **Декоратор (Decorator):** Динамически добавляет объекту новые обязанности (обертка).
- **Фасад (Facade):** Предоставляет простой интерфейс к сложной системе классов/библиотеке.
- **Легковес (Flyweight):** Позволяет вместить большее количество объектов в выделенную память, разделяя общее состояние между ними.
- **Заместитель (Proxy):** Объект-прослойка, контролирующий доступ к другому объекту (ленивая инициализация, логирование, кэширование).

---

## 🧠 Поведенческие (Behavioral)

Отвечают за эффективное взаимодействие и распределение обязанностей между объектами.

- **Цепочка обязанностей (Chain of Responsibility):** Передает запрос по цепочке обработчиков.
- **Команда (Command):** Превращает запрос в объект, позволяя передавать его как аргумент, ставить в очередь или логировать.
- **Итератор (Iterator):** Дает способ последовательного обхода элементов коллекции без раскрытия ее внутреннего представления.
- **Посредник (Mediator):** Убирает прямые связи между объектами, заставляя их общаться через посредника.
- **Снимок (Memento):** Позволяет сохранить и восстановить внутреннее состояние объекта, не нарушая инкапсуляции.
- **Наблюдатель (Observer):** Механизм подписки, оповещающий объекты об изменении состояния наблюдаемого объекта.
- **Состояние (State):** Позволяет объекту менять свое поведение в зависимости от внутреннего состояния.
- **Стратегия (Strategy):** Определяет семейство алгоритмов и позволяет менять их на лету.
- **Шаблонный метод (Template Method):** Определяет скелет алгоритма, перекладывая реализацию шагов на подклассы.
- **Посетитель (Visitor):** Позволяет добавлять новые операции для иерархии классов без изменения самих классов.
- **Интерпретатор (Interpreter):** Решает часто повторяющуюся задачу, определяя грамматику языка (используется редко).
